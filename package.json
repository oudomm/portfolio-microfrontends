{
  "name": "micro-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm build:remotes && concurrently \"pnpm preview:remotes\" \"pnpm watch:remotes\" \"pnpm dev:shell\"",
    "dev:shell": "pnpm --filter shell dev",
    "build:remotes": "pnpm --filter '!shell' build",
    "watch:remotes": "pnpm --filter '!shell' --parallel watch",
    "preview:remotes": "pnpm --filter '!shell' --parallel preview",
    "build": "pnpm -r build",
    "preview": "pnpm -r --parallel preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
